# Delete trailling spaces
delete_trailing_spaces () {
  export LC_CTYPE=C
  export LANG=C
  find $1 -not \( -name .svn -prune -o -name .git -prune \) -type f -print0 | \
    xargs -0 sed -i '' -E "s/[[:space:]]*$//"
}
